# SESSION_SECRET is filled in by deploy-k8s.sh (openssl rand -hex 32) and
# applied via kubectl. Don't commit a populated copy of this file.
apiVersion: v1
kind: Secret
metadata:
  name: dbi-secret
  namespace: dbinterface
type: Opaque
stringData:
  SESSION_SECRET: __SESSION_SECRET__
